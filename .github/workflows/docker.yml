name: Build and publish Docker image

# Builds ghcr.io/jhytabest/shoberfredy on every pull request and every master
# push (and tags). Only master pushes and tags publish.
#
# The build runs on pull requests because it is the only check that can fail for
# a reason lint cannot see, and because Renovate automerges on a green check set.
# When this ran on master alone, `numpy==2.5.1` sailed through review and merged:
# it needs Python 3.12 and the base image ships 3.11, so master's image stopped
# building, no new digest was published, and the homeserver — which pins a digest
# — simply stopped receiving updates with nothing failing in front of anyone.
#
# Deployment to the homeserver stays in the homeserver repo, where Renovate
# advances the published master digest through the normal release path.
#
# Every third-party action is pinned to a commit SHA, not a tag. CloakBrowser
# is lockfile-pinned too and updated by Renovate, so every image is reproducible.

on:
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - 'master'
    tags:
      - '*'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  quality:
    name: Lint and format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # From .nvmrc, not a bare major. `node-version: 24` resolved to whatever
      # patch setup-node's manifest happened to carry (24.18.0), so an engines
      # floor of >=24.19.0 failed yarn at "Validating package.json" before it
      # fetched anything. One file now decides what CI installs, what the image
      # ships and what the floor demands, so Renovate can move all three together.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - run: yarn install --frozen-lockfile

      - name: Check formatting
        run: yarn format:check

      - name: Lint
        run: yarn lint

  build-and-push-image:
    needs: quality
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      # Only the publishing runs need the registry. A pull request builds and
      # smoke-tests the image and never pushes it.
      - name: Log in to the Container registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=tag
            type=ref,event=pr
            type=raw,value=master,enable=${{ github.ref == 'refs/heads/master' }}

      # Homeserver is a single amd64 box; skipping arm64 halves build time.
      - name: Build Docker image for verification
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          load: true
          push: false
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # The homeserver deploy gates on this image's own HEALTHCHECK, so a build
      # that cannot reach a healthy /health must never be published. Exercises
      # the real startup path: CloakBrowser binary check, SQLite init, the full
      # migration run, worker startup. The API key is a placeholder — /health
      # only checks that one is configured, it makes no outbound call.
      - name: Smoke test the image
        env:
          IMAGE_TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          image=$(head -n1 <<< "$IMAGE_TAGS")
          container=$(docker run --detach \
            --env GOOGLE_GEOCODING_API_KEY=smoke-test-placeholder \
            --env FREDY_MARKET_EXPORTER_PORT=0 \
            --env FREDY_MARKET_MODEL_CRON=0 \
            "$image")
          trap 'docker rm --force "$container" >/dev/null 2>&1 || true' EXIT
          for _ in $(seq 1 60); do
            status=$(docker inspect "$container" --format '{{.State.Health.Status}}')
            [ "$status" = healthy ] && break
            if [ "$status" = unhealthy ] || [ "$(docker inspect "$container" --format '{{.State.Running}}')" != true ]; then
              echo "::error::container became $status before reporting healthy"
              docker logs "$container"
              exit 1
            fi
            sleep 5
          done
          if [ "${status:-}" != healthy ]; then
            echo "::error::container never reported healthy"
            docker logs "$container"
            exit 1
          fi
          docker exec "$container" curl -fsS --max-time 10 http://localhost:9998/health

      - name: Publish Docker image
        if: github.event_name != 'pull_request'
        env:
          IMAGE_TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          while IFS= read -r tag; do
            [ -n "$tag" ] && docker push "$tag"
          done <<< "$IMAGE_TAGS"
