# The image is node:24-slim, which is Debian 12 and ships Python 3.11, so
# 2.4.x is the newest numpy that installs here at all: 2.5 declares
# Requires-Python >=3.12 and pip refuses it. Renovate is told the same thing
# in renovate.json via constraints.python plus constraintsFiltering: strict —
# the constraint alone declares the interpreter without filtering anything, and
# Renovate reopened a 2.5.1 PR five minutes after this pin landed to prove it.
numpy==2.4.6
lightgbm==4.7.0
